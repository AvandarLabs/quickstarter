import { createTheme } from "@mantine/core";

/**
 * The Mantine theme for the application. Extend this with brand colors,
 * fonts, and component defaults as the project grows.
 */
export const theme = createTheme({});
